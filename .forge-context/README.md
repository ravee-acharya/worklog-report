# Vendored Forge Context

This directory contains a built snapshot of the internal `@my/forge-context` package.

It is copied here so the app template can run Forge tests without workspace package access.

## Do Not Edit Manually

Files in this directory are overwritten by `yarn sync:testing-framework`.
